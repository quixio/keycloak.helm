apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "keycloak.fullname" . }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "keycloak.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        checksum/postgresql-secret: {{ include (print $.Template.BasePath "/postgresql-secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "keycloak.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "keycloak.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.keycloak.themes.enabled }}
      initContainers:
      - name: theme-provider
        image: {{ .Values.keycloak.themes.initContainer.image | default "busybox:1.36-musl" }}
        {{- with .Values.keycloak.themes.initContainer.command }}
        command:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        volumeMounts:
          - name: {{ .Values.keycloak.themes.volumeName | default "keycloak-themes" }}
            mountPath: /themes
          {{- with .Values.keycloak.themes.initContainer.volumeMounts }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        {{- with .Values.keycloak.themes.initContainer.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        args:
          - start
          {{- range .Values.keycloak.extraArgs }}
          - {{ . }}
          {{- end }}
        env:
          # Admin credentials
          - name: KEYCLOAK_ADMIN
            value: {{ .Values.keycloak.admin.username | quote }}
          - name: KEYCLOAK_ADMIN_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ include "keycloak.adminSecretName" . }}
                key: {{ .Values.keycloak.admin.existingSecretKey | default "password" }}
          
          # Database configuration
          - name: KC_DB
            value: {{ .Values.keycloak.database.vendor | quote }}
          - name: KC_DB_URL_HOST
            value: {{ include "keycloak.databaseHost" . | quote }}
          - name: KC_DB_URL_PORT
            value: {{ .Values.keycloak.database.port | quote }}
          - name: KC_DB_URL_DATABASE
            value: {{ .Values.keycloak.database.database | quote }}
          - name: KC_DB_USERNAME
            valueFrom:
              secretKeyRef:
                name: {{ include "keycloak.databaseSecretName" . }}
                key: {{ .Values.keycloak.database.existingSecretUsernameKey | default "username" }}
          - name: KC_DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ include "keycloak.databaseSecretName" . }}
                key: {{ .Values.keycloak.database.existingSecretPasswordKey | default "password" }}
          
          # Connection pool settings
          - name: KC_DB_POOL_INITIAL_SIZE
            value: {{ .Values.keycloak.database.poolInitialSize | quote }}
          - name: KC_DB_POOL_MAX_SIZE
            value: {{ .Values.keycloak.database.poolMaxSize | quote }}
          - name: KC_DB_POOL_MIN_SIZE
            value: {{ .Values.keycloak.database.poolMinSize | quote }}
          
          # Java options
          - name: JAVA_OPTS_APPEND
            value: "-Djgroups.dns.query={{ include "keycloak.fullname" . }}-headless"

          # Proxy configuration (when running behind reverse proxy)
          {{- if .Values.keycloak.proxy.enabled }}
          - name: KC_HTTP_ENABLED
            value: "true"
          - name: KC_PROXY_HEADERS
            value: {{ .Values.keycloak.proxy.headers | quote }}
          {{- end }}

          # Hostname configuration
          - name: KC_HOSTNAME
            value: {{ .Values.keycloak.hostname.url | quote }}
          - name: KC_HOSTNAME_STRICT
            value: {{ .Values.keycloak.hostname.strict | quote }}
          {{- if .Values.keycloak.hostname.relativePath }}
          - name: KC_HTTP_RELATIVE_PATH
            value: {{ .Values.keycloak.hostname.relativePath | quote }}
          {{- end }}

          # Health and metrics
          {{- if .Values.keycloak.health.enabled }}
          - name: KC_HEALTH_ENABLED
            value: "true"
          {{- end }}
          {{- if .Values.keycloak.metrics.enabled }}
          - name: KC_METRICS_ENABLED
            value: "true"
          {{- end }}

          # Cache configuration
          {{- if .Values.keycloak.cache.stack }}
          - name: KC_CACHE_STACK
            value: {{ .Values.keycloak.cache.stack | quote }}
          {{- end }}

          # Additional custom environment variables
          {{- with .Values.keycloak.extraEnv }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        
        ports:
          - name: http
            containerPort: 8080
            protocol: TCP
          - name: https
            containerPort: 8443
            protocol: TCP
          {{- if .Values.keycloak.metrics.enabled }}
          - name: metrics
            containerPort: 9000
            protocol: TCP
          {{- end }}

        

        resources:
          {{- toYaml .Values.resources | nindent 10 }}

        volumeMounts:
        {{- if .Values.persistence.enabled }}
          - name: data
            mountPath: /opt/keycloak/data
        {{- end }}
        {{- if .Values.keycloak.themes.enabled }}
          - name: {{ .Values.keycloak.themes.volumeName | default "keycloak-themes" }}
            mountPath: /opt/keycloak/themes
        {{- end }}
      
      {{- if or .Values.persistence.enabled .Values.keycloak.themes.enabled }}
      volumes:
      {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "keycloak.fullname" . }}
      {{- end }}
      {{- if .Values.keycloak.themes.enabled }}
        - name: {{ .Values.keycloak.themes.volumeName | default "keycloak-themes" }}
          emptyDir: {}
        {{- with .Values.keycloak.themes.configMap }}
        - name: theme-config
          configMap:
            name: {{ . }}
        {{- end }}
      {{- end }}
      {{- end }}
      
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

